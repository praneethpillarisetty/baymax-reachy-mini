# Linux ARM64 target

Run `scripts/setup_reachy.sh` first. This directory intentionally contains no binary and no SDK package: the shared wheel must be embedded through the currently documented official Reachy Mini app workflow after its package metadata is verified.
