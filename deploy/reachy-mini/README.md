# Reachy Mini application target

Placeholder for the **official generated Reachy Mini application template**. Do not replace it with a custom executable. The build environment could not fetch the current official template or root `AGENTS.md`; adding guessed entry points would be unsafe. Generate the template with the current official CLI, then add `baymax-reachy-mini[reachy]` and wire its lifecycle to `ReachyMiniRobot`.

Current verification attempt (2026-08-06): the documentation tool returned HTTP 401 and direct GitHub access returned HTTP 403. No metadata or lifecycle file is placed here until the official generator output can be inspected. This directory is therefore **not an app package** and is not deployable.
